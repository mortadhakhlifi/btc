#include <stdio.h>
#include <string.h>

typedef struct {
    char firstname[20];
    char lastname[20];
    char ID[20];
    char BTC[20];
    int BD, BM, BY;
    int gender;
    int role;
    int T[3];
  } user;